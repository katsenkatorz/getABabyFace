<header class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-ex-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('admin_homepage') }}"><span>{{ 'admin'|trans }}</span></a>
        </div>
        <div class="collapse navbar-collapse" id="navbar-ex-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li {% if '/admin/album' in app.request.pathInfo  %}class="active"{% endif %}>
                    <a href="{{ path('album_index') }}">{{ 'album.albums'|trans }}</a>
                </li>
                <li {% if '/admin/post' in app.request.pathInfo  %}class="active"{% endif %}>
                    <a href="{{ path('post_index') }}">{{ 'post.posts'|trans }}</a>
                </li>
                <li {% if '/admin/user' in app.request.pathInfo  %}class="active"{% endif %}>
                    <a href="{{ path('user_index') }}">{{ 'user.users'|trans }}</a>
                </li>
            </ul>
            <a class="btn btn-default navbar-btn" href="{{ path('main_homepage') }}">{{ 'back_to_site'|trans }}</a>
        </div>
    </div>
</header>