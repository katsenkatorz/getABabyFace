{% extends "AdminBundle::layout.html.twig" %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}
{% block admin_body %}
    <h1>{{ 'album.create_album'|trans }}</h1>
    <div class="row">
        <div class="col-md-12">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success pull-right" title="{{ 'album.create_album'|trans }}" type="submit"><i class="material-icons md-36">check</i></button>
            {{ form_end(form) }}
        </div>
    </div>
    <hr>
    <div class="pull-right row">
        <div class="col-md-12">
            <a class="btn btn-default" title="{{ 'back_to_list'|trans }}" href="{{ path('album_index') }}"><i class="material-icons md-36">arrow_back</i></a>
        </div>
    </div>
{% endblock %}
